/* Manejo del DOM */
const mostrar =POKEMON.pokemon;
for (let index = 0; index < mostrar.length; index++) {
    const element = mostrar[index];
    }

console.log(mostrar[0].name);
    
//console.log(element.name);